"""from design.point import Point
from design.testing import listOfPoints


class layer(listOfPoints: list[Point]):

    A layer is a list of points that define a enclosed shape, meaning that the first point is the same as the last point
    Args:
        listOfPoints (list[Point]): list of points that define the shape


    def __init__(self, listOfPoints: list[Point]):
        self.listOfPoints = listOfPoints

    def __str__(self):
        return f"{self.listOfPoints}"
"""